import { MockBrowserProvider } from "./mock_browser_provider";

export class PlaywrightBrowserProvider extends MockBrowserProvider {
  // Placeholder wrapper. Add Playwright dependency and implementation here when live browser mode is enabled.
}

