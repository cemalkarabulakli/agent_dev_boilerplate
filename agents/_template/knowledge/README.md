# Knowledge

Add local source material here.

- Put untouched files in `raw/`.
- Put reviewed summaries or extracted notes in `processed/`.
- Register every source in `sources.yaml`.
- Do not treat unsourced notes as facts.
