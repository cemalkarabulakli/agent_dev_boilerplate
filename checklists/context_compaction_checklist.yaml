items:
  - id: keep_recent_turns
    category: "Context Compaction"
    description: "Recent user turns are preserved verbatim."
    severity: critical
    check: context_compaction
  - id: preserve_durable_facts
    category: "Context Compaction"
    description: "Goals, constraints, decisions, dates, numbers, sources, and open tasks are preserved."
    severity: critical
    check: context_compaction
  - id: keep_raw_history
    category: "Context Compaction"
    description: "Raw history is append-only and not replaced by compacted context."
    severity: critical
    check: context_compaction
