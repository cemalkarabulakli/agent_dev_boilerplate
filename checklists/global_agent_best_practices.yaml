items:
  - id: agent_structure
    category: "Agent Structure"
    description: "Required agent files and directories exist."
    severity: critical
    check: agent_structure
  - id: prompt_quality
    category: "Prompt Quality"
    description: "System prompt contains all required sections."
    severity: critical
    check: prompt_quality
  - id: context_compaction
    category: "Context Compaction"
    description: "Context settings, raw history, compactor, and prompt are present."
    severity: critical
    check: context_compaction
  - id: knowledge_sources
    category: "Knowledge"
    description: "Knowledge sources have IDs, types, URLs, and safe placeholder handling."
    severity: critical
    check: knowledge_sources
  - id: tool_safety
    category: "Safety"
    description: "Tool permissions and output guardrails are explicit."
    severity: critical
    check: tool_safety
  - id: github_ready
    category: "GitHub"
    description: "Workflows and environment hygiene files are present."
    severity: critical
    check: github_ready
  - id: no_secrets
    category: "Safety"
    description: "Repository does not contain API-key-like committed secrets."
    severity: critical
    check: no_secrets
